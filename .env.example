# Hevy API key (https://api.hevyapp.com/docs) — used at build time for the training heatmap.
# Add the same value in Vercel → Project → Settings → Environment Variables.
HEVY_API_KEY=your-hevy-api-key
